describe('Custom token extractor interceptor', function () {
  it('getExtractorForNonBlackListedTokens: Should get original bearer token extractor function when token is NOT blacklisted');

  it('getExtractorForNonBlackListedTokens: It should get a function that returns null when the token is blacklisted');
});
