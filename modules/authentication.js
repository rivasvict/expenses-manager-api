const signUp = () => {};
const isEmailDuplicated = async (email) => {
  await console.log(email);
  return email;
};

const authentication = { signUp, isEmailDuplicated };

module.exports = authentication;
